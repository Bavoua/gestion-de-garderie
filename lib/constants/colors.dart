import 'package:flutter/material.dart';

class AppColor {
  static const themeColor = Colors.lightBlue;
  static const btnColor = Color(0xFF5377B0);
  static MaterialColor textColor = const MaterialColor(
      0xFF000000, <int, Color>{50: Colors.indigo, 60: Colors.red});
}
